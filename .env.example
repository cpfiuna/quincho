# Archivo de entorno de ejemplo para Quincho FIUNA
# Copiá este archivo como `.env` y completá los valores reales de los secretos localmente.

# Variables de entorno del proyecto Supabase
# Reemplazá con la URL de tu proyecto Supabase (incluyendo https://)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co/

# Reemplazá con tu clave anónima de Supabase (clave pública para el cliente)
VITE_SUPABASE_ANON_KEY=REPLACE_WITH_PUBLIC_ANON_KEY

# Reemplazá con tu Service Role Key de Supabase (MANTENER SECRETO - no subir al repositorio)
VITE_SUPABASE_SERVICE_KEY=REPLACE_WITH_SERVICE_ROLE_KEY

# Opcional: Token de acceso usado por CI para desplegar funciones (MANTENER SECRETO)
# Nota: en el repositorio la variable se llamaba `VITA_SUPABASE_ACCESS_TOKEN` (error tipográfico);
# usá un nombre claro cuando lo agregues a los secretos del CI, por ejemplo SUPABASE_ACCESS_TOKEN
VITA_SUPABASE_ACCESS_TOKEN=REPLACE_WITH_SUPABASE_ACCESS_TOKEN

# URL de la app usada en correos y redirecciones (incluyendo https://)
APP_URL=https://quincho.cpfiuna.io

# Configuración de zona horaria
TIMEZONE=America/Asuncion

# Configuración del servicio de correo (Resend)
RESEND_API_KEY=REPLACE_WITH_RESEND_API_KEY
EMAIL_SENDER=quincho-noreply@cpfiuna.io

# ------------------------------
# USO
# 1) Copiá este archivo como `.env`:
#    cp .env.example .env
# 2) Completá los secretos reales en tu `.env` local (nunca subas `.env`).
# 3) Agregá los mismos secretos en tu proveedor de despliegue (Vercel/GitHub Actions/Supabase) como variables de entorno seguras.
# ------------------------------
